<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- package="com.perfectcorp.usb2hdmi" REMOVIDO -->

    <!--
         Permissões:
         - FOREGROUND_SERVICE é provavelmente necessário para manter a MediaProjection
           estável, especialmente em versões mais recentes do Android.
           A permissão específica FOREGROUND_SERVICE_MEDIA_PROJECTION é adicionada
           automaticamente pelo sistema quando targetSdk >= 29 e a projeção é iniciada.
         - A permissão para MediaProjection em si é solicitada em tempo de execução.
         - Permissões USB seriam necessárias se usássemos UsbManager ativamente.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Se precisarmos de um serviço de projeção de mídia em primeiro plano (Android 14+) -->
    <!-- <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" /> -->


    <!--
         Recursos de Hardware:
         - usb.host indica que o app PODE interagir com dispositivos USB host.
           required="false" significa que o app pode ser instalado mesmo sem suporte a host USB,
           mas a funcionalidade relacionada pode não funcionar. Importante se a detecção
           do adaptador via UsbManager for implementada como um fallback ou recurso adicional.
    -->
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.USB2HDMIPerfeito"
        tools:targetApi="34">

        <!-- Declaração da MainActivity -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                 Opcional: Intent filter para tentar abrir o app quando um
                 dispositivo HDMI é conectado via USB-C. A eficácia disso
                 pode variar entre dispositivos/versões do Android.
                 Requer testes cuidadosos.
            -->
            <!--
            <intent-filter>
                <action android:name="android.hardware.hdmi.action.HDMI_PLUGGED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                 <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                       android:resource="@xml/device_filter" />
            -->

        </activity>

        <!--
             Declaração de Serviço (se necessário):
             Se a MediaProjection for executada em um Foreground Service para maior
             estabilidade, ele seria declarado aqui.
        -->
        <!--
        <service
            android:name=".service.ProjectionService"
            android:foregroundServiceType="mediaProjection"
            android:exported="false" />
        -->

    </application>

</manifest>